{
  "name": "@claude-view/design-tokens",
  "version": "0.45.3",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "mkdir -p dist && printf 'source-consumed workspace package\\n' > dist/build-stamp.txt && echo 'No build step — consumed as TypeScript source'",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  }
}
